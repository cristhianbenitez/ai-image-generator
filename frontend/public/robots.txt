# Allow all web crawlers
User-agent: *
Allow: /

# Sitemap location
Sitemap: %VITE_APP_URL%/sitemap.xml

# Prevent access to API routes
Disallow: /api/

# Prevent access to authentication routes
Disallow: /auth/

# Add crawl delay to prevent overwhelming the server
Crawl-delay: 10
